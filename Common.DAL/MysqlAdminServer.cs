//using MySql.Data.MySqlClient;

namespace Common.DAL
{
    public class MysqlAdminServer
    {

        //public static string connectStr = "server=localhost;port=3306;database=esmsdb;user=root;password="; // 用户名和密码在MySQL定义的
        //public int GetEffectNumber(string sql)
        //{
        //    //// 创建连接
        //    //MySqlConnection conn = new MySqlConnection(connectStr);
        //    //conn.Open();
        //    //return MySqlHelper.ExecuteNonQuery(connectStr, CommandType.Text, sql);
        //}
        //public DataTable GetTable(string sql)
        //{
        //    创建连接
        //   MySqlConnection conn = new MySqlConnection(connectStr);
        //    conn.Open();
        //    return MySqlHelper.ExecuteDataset(connectStr, CommandType.Text, sql).Tables[0];
        //}
    }
}
